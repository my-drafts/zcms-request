#!/usr/bin/env node
'use strict';

const url = require('url');
const uf = require('util').format;
const zt = require('ztype');

class zRequest{
	constructor(options){

	}
};

const zrequest = function(request, options){
	let result = zRequest(options);
	console.log(res);
	return result;
};

module.exports = zrequest;

